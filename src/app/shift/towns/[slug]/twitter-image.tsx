/**
 * Twitter/X large card image — re-exports the OG image generator so both
 * og:image and twitter:image resolve to the same 1200×630 card.
 */
export { default, size, contentType } from './opengraph-image'
